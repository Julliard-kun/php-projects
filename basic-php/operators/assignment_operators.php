<?php
    "This is about assignment operators.";

    "Assignment operators are used to assign values to variables.";

    "There are 10 assignment operators in PHP.";

    "These are =, +=, -=, *=, /=, %=, .= .";

    $assignment_operator = 1; // = is the assignment operator.

    $plus_equal = 1;
    $plus_equal += 1; // += is the plus equal operator.
    echo $plus_equal;
    echo "<br>";

    $minus_equal = 1;
    $minus_equal -= 1; // -= is the minus equal operator.
    echo $minus_equal;
    echo "<br>";

    $multiply_equal = 2;
    $multiply_equal *= 2; // *= is the multiply equal operator.
    echo $multiply_equal;
    echo "<br>";

    $divide_equal = 4;
    $divide_equal /= 2; // /= is the divide equal operator.
    echo $divide_equal;
    echo "<br>";

    $modulus_equal = 1;
    $modulus_equal %= 1; // %= is the modulus equal operator.
    echo $modulus_equal;
    echo "<br>";

    $concatenate_equal = 1;
    $concatenate_equal .= 1; // .= is the concatenate equal operator.
    echo $concatenate_equal;
    echo "<br>";

?>